 <div class="container-fluid" id="small-banner"> 
        </div>
        {{#each data}}
        <div class="container py-5">
            <h3 class="text-dark">{{name}}</h3>
            <div> {{{description}}}</div>
        </div>
        {{/each}}